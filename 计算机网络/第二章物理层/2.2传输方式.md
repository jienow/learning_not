# 串行传输与并行传输
1. 串行传输：比特一次传输
2. 并行传输：多条线并行传输
3. 串行传输适合远距离主机对主机传输，并行传输适合主机内部传输，比如cpu与内存之间传输
# 同步传输和异步传输
1. 同步传输：所有数据一起传输
	1. 外同步：在收发双方添加一条单独的时钟信号线
	2. 内同步：发送端将时钟信号编码到数据中一起传输
2. 异步传输：一个字节一个字节进行传输，字节内部时间间隔固定，字节之间不固定
# 单工通信、半双工通信、全双工通信
1. 单工通信：只能但方向传播
2. 半双工通信：可以双向通信但是只能一次只能传输一个方向的
3. 全双工通信：两个方向的可以任意通信